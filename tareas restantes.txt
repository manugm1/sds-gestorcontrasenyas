
PARTE DE GENERACION DE CONTRASEÑA ALEATORIA DEL USUARIO

Olvídate de la parte de login, solo tendrás que tocar la parte de REGISTRO en el client.go

Créate una función que sirva de cuestionario para el usuario.

UN EJEMPLO DE CODIGO

func registro(){
	//Introducir usuario y contraseña
	fmt.Println("Introduce email: ")
	email := leerStringConsola()

	//NUEVO: Antes de introducir la contraseña, el sistema preguntará
	fmt.Println("¿Generar contraseña aleatoria? S/N")
	respuestaPassword  := leerStringConsola()

	if respuestaPassword == "S"
		password := generarContrasenyaAleatoria()
	else {
		fmt.Println("Introduce contraseña: ")
		password := leerStringConsola()
	}
		
		
}

func generarContrasenyaAleatoria(){
	//pREGUNTAS
	fmt.Println("¿Nº de caracteres?")
	fmt.Println("¿Añadir números?")
	... LO QUE SE TE OCURRA
	GENERAR CONTRASEÑA ALEATORIA SEGÚN LO QUE HA ELEGIDO EL USUARIO
	return contrasenyaAleatoria
} //que devuelva la contraseña aleatoria


PARTE DE OBTENER USUARIO POR ID

- Quita del menú esa opción, ya que no debe ser público para el usuario.
- Los métodos editar entrada y eliminar entrada deben utilizar el método obtener usuario por id pero antes de eso, llamar a Listar entradas.

Por ejemplo, si el usuario elige editar una entrada, primero le aparecerá el listado de entradas y después, permitir elegir un id de la entrada.
Lo mismo para borrar una entrada.

